# mr5am.github.io

## Local Development

Run the static site locally:

```bash
npm run dev
```

Then open:

```text
http://127.0.0.1:5173/
```

The dev server supports client-side routes such as `/bloom`, `/core`, `/reclaim`, and `/beyond`.
